# Session 2026-09-26 06:11 Asia/Jakarta

Goal: execute Gates 0 and 1 of `docs/product/NETWORK_MEMBERSHIP_ROLLOUT_PLAN.md` in Franchisor.id — the Gate 0 audit artifacts and the Gate 1 ownership/membership write-path parity against the deployed shared D1 guard chain.

Scope agreed before work: Gates 0–1 code plus artifacts; read-only live D1 access; the narrow pilot policy; and an amended SEO decision (franchisor.id uses `/usaha/{slug}` for legacy and new brands, a family unique to this site). Gate 2–5 provider work stays out of scope as step-by-step operator instructions.

## Evidence

Read-only live D1 access used the `franchise-network` entry in `%APPDATA%\cfman\tokens.json`; no secret value was printed, copied into Markdown, or committed, and no D1 write was issued.

- Migration ledger holds 35 rows (ids 1–33, 37, 38) while the objects created by 0035, 0036, and 0039 are live with no ledger row. The guard chain is deployed and effective.
- `franchises`: 197 rows — 196 `UNCLAIMED`/`unclaimed` and 1 `FRANCHISOR`/`free` (`Kopi Coba`, owner NULL).
- `franchise_site_publications`: 197 published for `site_franchisee_id`, **0 for `site_franchisor_id`** in any status.
- `franchise_claims` 0 rows, `franchise_submission_reviews` 0 rows, and 0 owner-review proposals.
- `site_rebuild_requests` holds 35 rows stuck in `queued` since 2026-08-14 with `last_published_at` NULL.
- The pollers in both repositories read `site_publish_requests`, which exists in no migration and not in the deployed schema; they also read `published_today` and write `last_error`, neither of which exist.
- Anonymous production check: `/this-does-not-exist-12345/` returns **200** with the same 333,814-byte legacy directory page as `/auth-config`, `/profil/`, `/dashboard/`, `/daftar/`, and `/premium/`. The domain is a soft-404 catch-all, so route status is not a readiness signal.
- `/usaha/{slug}` and `/usaha/{slug}/` serve the real brand page and self-declare `canonical='/usaha/{slug}'`; `/peluang-usaha/{slug}` returns the catch-all.
- 34 legacy `/usaha/*` directories matched 34 canonical `franchises.id`; 33 by exact slug, 1 by normalized name (`pisang-molen-m-a` → canonical slug `pisang-molen-ma`).

## Changed

Gate 0 artifacts: `docs/product/FRANCHISOR_PARITY_MATRIX.md`, `docs/operations/PROVIDER_BOUNDARY_RECORD.md`, `docs/product/LEGACY_BRAND_MATCH.md`, `docs/product/NETWORK_MEMBERSHIP_PROGRESS.md`.

Gate 1 runtime: new `functions/_profile-owner-review.js`; rewritten claim and new-brand branches in `functions/_form-submit-franchisor.js`; `findExistingBrands` and guarded `findClaimSource` in `functions/_form-submit-utils.js`; review proposals on published edits in `functions/_profile-franchisor-actions.js` and `functions/profile-upload.js`; `handleReviewBrandSubmission` plus staleness/evidence/conflict guards in `functions/_dashboard-actions.js`; `review_brand_submission` in `functions/_dashboard-schemas.js` and `functions/dashboard-data.js`; review queues in `functions/_dashboard-queries.js`; the `/usaha/{slug}` canonical in `functions/_premium.js` and the brand-detail links across the read models.

Client: brand-submission review UI in `js/dashboard-review.js`, `js/dashboard-admin.js`, and `src/components/dashboard/DashboardReviewPanel.astro`; owner pending-review status and `/usaha/` link sweep across the profile and dashboard clients.

Publish queue: `scripts/d1-static-publish-poller.mjs` table, column, ordering, and mode corrections.

Tests and docs: new `scripts/check-ownership-contract.ts` with the `ownership:check` script gating `build:astro`; repaired pre-existing stale assertions in `scripts/check-state-transitions.ts`; `CHANGELOG.md`, `AGENTS.md`, `CODEBASE.md`, `docs/README.md`, the rollout plan, the journeys matrix, the network context, the data contract, and `docs/forms/AUTO_SAVE.md`.

## Verification

`pnpm run astro:check` returned 0 errors, 0 warnings, 5 hints. All 16 `*:check` feature scripts passed, including the new `ownership:check`. `node scripts/test-d1-static-publish-poller.mjs` passed. `pnpm run build` completed with 12 Astro pages, 5,001 deployed files, and `Built asset check passed`, gated by `ownership:check`.

## Not done — deliberately

Gate 2–5 are untouched: no Pages project, Clerk satellite, deploy hook, secret, or live deployment change. Per-brand deployment-state UI (Gate 3.3) is not modelled. The generated brand route has not been relocated to `usaha/[slug]`, the legacy `/usaha/*` files are intentionally retained (they are the only working brand pages while zero franchisor rows are published), the soft-404 catch-all is unresolved, and the `/usaha/pisang-molen-m-a` redirect is deferred because its target is currently a soft-404. `../Franchisee.id/functions/_premium.js` still needs the matching canonical mapping, and the Franchisee.id poller copy still needs the same table/column correction.

## Next

Gate 2 in the provider boundary record §6, in order: create or confirm the `franchisor-id` Pages project as a Git-integrated project, add the D1/R2 bindings for Production and Preview, set the build and runtime variables and secrets, configure the Clerk satellite and webhook, map the custom domains, add the GitHub secrets and Deploy Hook, reconcile the migration ledger, then run the controlled acceptance matrix in the journey document.
