# Session handoff — Franchisor application adaptation

Timestamp: 2026-07-22 05:23 Asia/Jakarta

> Historical handoff with verified build evidence. Current-state documents were reconciled later on 2026-07-22; use `../CODEBASE.md` and `../docs/README.md` before acting on this snapshot.

## Outcome

Franchisee.id's mature Astro/Cloudflare application runtime was ported into Franchisor.id and adapted into a hybrid application that preserves the existing Franchisor WordPress-exported site. Site-scoped code uses `site_franchisor_id` and `franchisor.id`; shared network code still recognizes every network site and the existing shared `assets.franchisee.id` R2 hostname.

The application styling was explicitly adapted to the existing Franchisor.id design rather than left as Franchisee styling. `css/franchisor-theme.css` supplies the Franchisor red/ink palette, DM Sans/Lexend typography, and existing Franchisor logo assets to new app/auth/dashboard/profile/Premium routes.

## Data evidence

- Shared D1 database ID: `812cd8ac-edd0-45d9-981f-c9a15358317b`.
- Remote read-only inspection found 197 published Franchisee rows and zero Franchisor publication rows.
- Canonical records found: one free and 196 unclaimed; active Premium subscriptions: zero.
- Therefore the current empty generated Franchisor directory is consistent with the Premium-only publication policy. No canonical brands or site-publication rows were duplicated.

## Important decisions

- Do not add a Franchisor migration chain. Franchisee.id still owns shared migrations.
- Do not mass-copy Franchisee publication rows. Franchisor visibility requires explicit, auditable `site_franchisor_id` publication rows.
- Preserve legacy pages/assets until redirect, canonical, and crawl verification is complete.
- Premium email dispatch remains manual-only in this repository to prevent duplicate scheduled lifecycle messages.
- The browser auth client has no hard-coded Clerk key. A shared Clerk tenant requires explicit Franchisor satellite configuration.

## Verification

- Dependency install with frozen pnpm lockfile completed.
- D1 snapshot generation completed and returned zero Franchisor listings as expected.
- Final `pnpm run astro:check` completed with 0 errors and five inherited non-blocking hints.
- All 15 feature-contract checks passed: country data, auth, profile, contact parsing, directory, detail assets, Function methods, proposals, Google Contacts, Premium lifecycle, state transitions, proposal download, OCR configuration/runner, and dashboard OCR.
- Final `pnpm run build` passed after the CSS/auth changes: the D1 fetch returned zero Franchisor rows, Astro built 12 pages, and the legacy bridge copied 4,706 files.
- Built-route inspection confirmed the Franchisor theme and brand assets on login, registration, customer cabinet, dashboard, profile, Premium, and directory routes, with no copied Franchisee name/logo or yellow palette remaining in those application outputs.
- The build now ends with `assets:check`; the final run validated 4,887 deployed files and confirmed every reachable local HTML/CSS asset reference resolves with exact casing.
- Obsolete WordPress runtime references and unused LatePoint assets were removed from generated output; broken legacy image filenames were remapped to existing exports.
- `docs/operations/MANUAL_SETUP_CHECKLIST.md` now contains dashboard-only Cloudflare Pages/Astro deployment instructions and direct asset verification steps.

## Manual launch gates

Follow `docs/operations/MANUAL_SETUP_CHECKLIST.md`. The main remaining actions are Cloudflare Pages project/bindings, Clerk satellite DNS and webhook configuration, GitHub secrets/variables and deploy hook, custom domains, and optional provider credentials. After setup, run live auth, upload, publication, and deploy smoke tests.
