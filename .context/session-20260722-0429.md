# Session 2026-07-22 — Franchisor network context bootstrap

> Historical snapshot: the recommended scaffold action below was completed later the same day. See `session-20260722-0523.md`, `../CODEBASE.md`, and `../docs/operations/MANUAL_SETUP_CHECKLIST.md` for current state.

## Objective

Give future Codex sessions opened in Franchisor.id enough verified context to build it as a separate product within the same Franchise Network as Franchisee.id.

## Work completed

- Read all 185 tracked Markdown files in `../Franchisee.id`: 54 core documents and 131 session snapshots.
- Read the initial shared network migration and inspected current Franchisee D1, R2, Astro, Premium, site-publication, queue, and generator configuration.
- Inspected the Franchisor repository and confirmed it is a static WordPress export with 89 HTML files, 38 route directories, and 34 `/usaha/` brand pages.
- Added the Franchisor repository instructions, codebase map, network context, data contract, phased build plan, suggestions, and changelog.

## Key conclusions

- `franchise_db` is the shared source of truth.
- `site_franchisor_id` is this site's stable network identifier.
- One `franchises` row can have several per-site `franchise_site_publications` rows.
- Franchisor public pages must select only published Franchisor rows and active canonical brands.
- Franchisor remains a separate deployment, theme, SEO surface, route system, and publish queue consumer.
- Clerk identity may be shared, but each domain needs explicit configuration and D1 must authorize server actions.
- Franchisee.id currently owns the shared migration chain; it must not be forked here.
- The legacy export should be bridged and migrated deliberately, not treated as the new data source.

## Repository state at start

- Branch: `main`
- Remote: `https://github.com/cfpages-admtravelbos/Franchisor.id.git`
- Working tree: clean before documentation was added.
- No pre-existing Markdown documentation.

## Recommended next action

Begin Phase 0 route/brand matching and Phase 1 Astro scaffold from `docs/architecture/FRANCHISOR_BUILD_PLAN.md`. Do not make D1 writes while matching legacy brands.

## Open decisions

- Final brand canonical route: `/usaha/{slug}` or `/peluang-usaha/{slug}`.
- Publication eligibility scope for the Franchisor directory.
- Dashboard ownership between this domain and a future shared console.
- Timing of a dedicated shared network package/repository.
