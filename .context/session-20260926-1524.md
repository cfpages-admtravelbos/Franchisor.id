# Session 2026-09-26 15:24 WIB — URL and Astro/Pages handoff

Scope: read-only review of both repositories' route and build source, then Markdown-only handoff. Source HEADs before edits: Franchisor `e09ef15`, Franchisee `baf2478`; remote `main` matched both before publication. No code, D1, provider, or deployment changes.

Verified from source: Franchisor directory `src/pages/peluang-usaha/index.astro`, detail `src/pages/usaha/[slug].astro`, old detail meta refresh at `src/pages/peluang-usaha/[slug].astro`; `src/lib/franchise-static.ts` links cards to `/usaha/{slug}`. Franchisee directory and detail routes remain under `/peluang-usaha/`. Franchisor `build:astro` runs D1 snapshot, Astro, non-overwriting legacy copy, and asset validation, with ownership/schema gates. The published new-slug build proof is recorded in `docs/product/ROLLOUT_CODE_REVIEW_2026-09-26.md`; repeatability, unpublished-row proof, existing-slug precedence, and deployed routing remain open.

Fresh anonymous production HTTP check after the documentation push: `/auth-config` and a nonexistent URL still returned the legacy directory with HTTP 200 and canonical `/`; `/peluang-usaha/` retained directory canonical `/peluang-usaha`, and `/usaha/abo-meatshop` retained brand canonical `/usaha/abo-meatshop`. Exact byte counts and titles are in `docs/operations/PROVIDER_BOUNDARY_RECORD.md`. No provider setting was read or changed.

Files updated: `CODEBASE.md`, `docs/README.md`, `docs/architecture/FRANCHISE_NETWORK_CONTEXT.md`, `docs/architecture/FRANCHISOR_BUILD_PLAN.md`, `docs/data/SHARED_DATA_CONTRACT.md`, `docs/operations/MANUAL_SETUP_CHECKLIST.md`, `docs/operations/PROVIDER_BOUNDARY_RECORD.md`, `docs/product/FRANCHISOR_PARITY_MATRIX.md`, `docs/product/NETWORK_MEMBERSHIP_PROGRESS.md`, `CHANGELOG.md`, and this session note. Added `docs/operations/ASTRO_CLOUDFLARE_BRAND_PUBLISH_PLAN.md` for the implementing harness. Companion Franchisee `CODEBASE.md`, `CHANGELOG.md`, and session note clarify its unchanged route family.

Next implementing step: follow the new plan through a repeatable fixture and Pages preview, then verify the live source SHA, Franchisor theme/assets, `/peluang-usaha/` directory, `/usaha/{slug}` detail, nonpublished 404, and per-site rebuild status before a paid pilot. Do not describe the historical HTTP 200 soft-404 as Astro acceptance.
