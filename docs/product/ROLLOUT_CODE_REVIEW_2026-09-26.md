# Franchisor.id rollout code review — 2026-09-26

Review of `main` at `4b2decd` against the [rollout plan](NETWORK_MEMBERSHIP_ROLLOUT_PLAN.md), [journeys](FRANCHISOR_USER_JOURNEYS.md), and the explicit Franchisor brand URL contract: **`https://franchisor.id/usaha/{slug}` with no trailing slash**. The remote `main` ref matched this commit at review time. `pnpm run build` passed locally and its D1 snapshot contained zero Franchisor publication rows; this review did not test signed-in production flows, mutate D1, or verify a new deployment. The directory and category routes under `/peluang-usaha/` are a separate product decision; the required correction here concerns **brand detail slugs**.

## Findings for the implementing harness

| ID | Priority | Evidence and effect | Required follow-up and acceptance |
| --- | --- | --- | --- |
| R1 | P0 before a published brand or paid pilot | `functions/_premium.js` and the new-brand submit handler write `/usaha/{slug}` as the publication canonical, but Astro still generates `src/pages/peluang-usaha/[slug].astro`. `src/lib/franchise-static.ts` links directory cards to `/peluang-usaha/{slug}` and writes that path in brand JSON-LD and breadcrumb JSON-LD; `src/lib/franchise-directory-document.ts` also writes it in the directory ItemList. `src/lib/franchise-premium-detail.ts` uses it for the proposal link. `templates/detail-franchise-tpl.html` still declares `/peluang-usaha/{SLUG}` as its canonical and Open Graph URL. After a new publication row is approved, the brand can be linked and indexed at the wrong path while its stored canonical points to a missing `/usaha/` page. The current production soft-404 makes HTTP 200 insufficient proof. | Move the **generated detail** route to `src/pages/usaha/[slug].astro` and update all generated brand links, canonical, Open Graph, and structured-data URLs to `/usaha/{slug}`. Keep the 34 legacy `/usaha/*` files while there are no generated replacements: `copy-legacy-static.mjs` already copies without overwriting Astro output. Test one synthetic published D1 row through the real build: the built `/usaha/{slug}` page must contain the matching canonical and its directory card must link there; an unpublished row must make no public detail. Verify an existing legacy slug still resolves, and use content/canonical checks to detect soft-404s. Decide and test a real redirect for old generated `/peluang-usaha/{slug}` links, without redirecting directory/category routes. |
| R2 | P1 before any CSV import or bridge publication | The available `import:csv:remote` command calls `publication()` in `scripts/import-csv-utils.ts`, which still emits `https://franchisor.id/peluang-usaha/${slug}/` with `publication_status: 'published'`; `scripts/import-csv-to-d1.ts` calls it at lines 252 and 318. The optional `build:d1:franchises:bridge` path still writes brand HTML under `peluang-usaha/`, and `scripts/d1-page-renderer.ts` generates wrong brand links and structured-data URLs. These are separate write/build paths missed by `ownership:check`. | Before either command is used, make the import URL contract `/usaha/{slug}` and decide whether remote import is still an authorized publication path under the current private-review rules; disable or guard it if not. Align the bridge generator or explicitly retire it. Add one command-level fixture checking the emitted import SQL and bridge manifest/HTML for the correct brand path. Do not run a remote import to test this. |
| R3 | P1 tracker accuracy | [Legacy brand match](LEGACY_BRAND_MATCH.md) proves 34 legacy paths match 34 canonical `franchises.id` values, but reports **zero** `site_franchisor_id` publication rows. Tracker step 0.3 asks for both a canonical brand and a Franchisor publication row, yet is marked ✅. The parity matrix also marks the brand URL family ✅ while R1–R2 remain. | Mark the publication-row portion of 0.3 incomplete and separate the completed 34/34 brand identity match from future row creation. Keep the URL-family **decision** complete, but do not mark its generated-route implementation or end-to-end test complete. A future Gate 4 review must reconcile each published row, canonical URL, built page, sitemap, and deployment SHA before calling it live. |
| R4 | P2 documentation cleanup | [Provider boundary record](../operations/PROVIDER_BOUNDARY_RECORD.md) §5 still says the Franchisor poller reads a nonexistent table and cannot run, although `eb94f7d` fixed `scripts/d1-static-publish-poller.mjs` to read `site_rebuild_requests`; the progress tracker records the local poller tests as passing. This is a historical observation presented as a current blocker. | Add a dated correction to the provider record: local poller/schema agreement is fixed at `eb94f7d`; actual GitHub workflow execution, deployment hook, and production Pages wiring remain unverified. Do not infer live publishing from the local fix. |

## Safe sequence from this review

1. Fix R1 and R2 in code with a single shared Franchisor brand-path rule where existing structure permits it; keep directory/category path decisions separate. Extend `ownership:check` or a focused build check to cover the real page output, import SQL, and optional bridge if retained.
2. Correct the tracker and provider record in R3–R4. Preserve the evidence that no Franchisor publication row existed at the dated D1 snapshot; re-query before making a current-count claim.
3. Only then resume Gate 2 provider deployment and Gate 1 signed-in acceptance. Do not begin a paid pilot until the synthetic build, deployed `/usaha/` page, legacy route, signed-in ownership, and per-site publication/deployment checks pass. The one known slug difference, `/usaha/pisang-molen-m-a` versus canonical `/usaha/pisang-molen-ma`, needs its 301 only after the target page really exists.

The local build pass confirms the current **zero-row** build, not the published-brand scenario. The existing `ownership:check` verifies canonical writes and selected UI links but does not exercise the Astro detail output, template metadata, CSV import, or bridge generator described above.
